import React from 'react';
import { BlockContainer } from './common/BlockContainer';
import { Blocks } from '../../enums';

export const SummaryTableBlock = (data: any) => {
  return <BlockContainer name={Blocks.SummaryTable}>SummaryTableBlock</BlockContainer>;
};

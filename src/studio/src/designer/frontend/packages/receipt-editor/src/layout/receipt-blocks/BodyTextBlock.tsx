import React from 'react';
import { BlockContainer } from './common/BlockContainer';
import { Blocks } from '../../enums';

export const BodyTextBlock = (data: any) => {
  return <BlockContainer name={Blocks.SummaryTable}>BodyTextBlock</BlockContainer>;
};
